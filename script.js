(()=>{const canvas=document.createElement('canvas'),ctx=canvas.getContext('2d'),main=document.querySelector('main'),toggle=document.getElementById('theme-toggle');if(!main||!ctx||!toggle)return;main.appendChild(canvas);Object.assign(canvas.style,{position:'fixed',top:0,left:0,pointerEvents:'none',zIndex:-1});const dots=[],mouse={x:0,y:0};let w,h;const resize=()=>{w=canvas.width=innerWidth;h=canvas.height=innerHeight;dots.length=0;for(let i=50;i--;)dots.push({x:Math.random()*w,y:Math.random()*h,vx:(Math.random()-.5)*2,vy:(Math.random()-.5)*2});};const animate=()=>{ctx.clearRect(0,0,w,h);const dark=document.body.classList.contains('dark');ctx.fillStyle=dark?'#666':'#ccc';for(const dot of dots){dot.x+=dot.vx;dot.y+=dot.vy;if(dot.x<0||dot.x>w)dot.vx*=-1;if(dot.y<0||dot.y>h)dot.vy*=-1;ctx.beginPath();ctx.arc(dot.x,dot.y,2,0,6.28);ctx.fill();const dist=Math.hypot(mouse.x-dot.x,mouse.y-dot.y);if(dist<150){ctx.strokeStyle=`rgba(${dark?'150,150,150':'200,200,200'},${1-dist/150})`;ctx.beginPath();ctx.moveTo(dot.x,dot.y);ctx.lineTo(mouse.x,mouse.y);ctx.stroke();}}requestAnimationFrame(animate);};addEventListener('mousemove',e=>{const rect=canvas.getBoundingClientRect();mouse.x=e.clientX-rect.left;mouse.y=e.clientY-rect.top;});toggle.onclick=()=>{document.body.classList.toggle('dark');toggle.textContent=document.body.classList.contains('dark')?'☀️':'🌙';};addEventListener('resize',resize);resize();animate();})();